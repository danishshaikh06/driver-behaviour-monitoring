# Deployment

Install the project with `pip install -e .`, ensure camera permissions are
granted, then launch a monitor with `driver-monitor <monitor>`. Keep downloaded
or trained weights in `models/`; that directory is intentionally ignored.
